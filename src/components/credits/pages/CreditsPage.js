import React from 'react';
import CreditsContent from '../modules/CreditsContent';

const CreditsPage = () => {
  return (
    <>
      <CreditsContent />
    </>
  );
};

export default CreditsPage;
