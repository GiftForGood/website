import React, { useEffect, useState } from 'react';
import { withRouter } from 'next/router';

const RegisterDonor = (props, state) => {
	return (
		<div>
      RegisterDonor
		</div>
	);
};

export default withRouter(RegisterDonor);
