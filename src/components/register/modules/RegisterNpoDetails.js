import React, { useEffect, useState } from 'react';
import { withRouter } from 'next/router';

const RegisterNpoDetails = (props, state) => {
	return (
		<div>
      RegisterNpoDetails
		</div>
	);
};

export default withRouter(RegisterNpoDetails);
