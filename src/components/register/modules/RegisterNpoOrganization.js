import React, { useEffect, useState } from 'react';
import { withRouter } from 'next/router';

const RegisterNpoOrganization = (props, state) => {
	return (
		<div>
      RegisterNpoOrganization
		</div>
	);
};

export default withRouter(RegisterNpoOrganization);
