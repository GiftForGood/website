const initialState = {
  user: null,
  selectedChatId: null,
  isNewChat: false,
  isViewingChatsForMyPost: false,
  hasError: false,
  postId: null,
  postType: null,
};

export default initialState;
